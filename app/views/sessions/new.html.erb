<div class='container'>
	<div class='row'>
		<p id="notice"><%= notice %></p>
		<%= simple_form_for(:session, :url => sessions_path) do |f| %>
		<%= f.input :email %>
		<%= f.input :password %>
		<%= f.submit "Login" %>
		<% end %>
	</div>
</div>